/// Technician / worker settings routes — same UI as admin profile & privacy,
/// opened via [EmployeePersonalProfilePage] (local data only, no profile API).
abstract final class EmployeeTechnicianSettingsRoutes {
  static const personalProfile = '/employee-role/settings/personal-profile';
  static const privacy = '/employee-role/settings/privacy';
}
