/// Temporary toggles for the drawing canvas toolbar (code stays; UI can be hidden).
class DrawingCanvasFeatureFlags {
  DrawingCanvasFeatureFlags._();

  /// Remove All / delete tool on the design (drawing) screen.
  static const bool showDeleteTool = false;
}
