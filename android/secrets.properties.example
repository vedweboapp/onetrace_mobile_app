# Copy to android/secrets.properties (gitignored) and set your key.
# Enable in Google Cloud (same project as this key):
#   Maps SDK for Android, Maps SDK for iOS, Places API, Geocoding API
# Blank map + Google logo only = Maps SDK for Android is not enabled / key blocked.
GOOGLE_MAPS_API_KEY=YOUR_GOOGLE_MAPS_API_KEY
